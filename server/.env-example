PORT=5001
JWT_SECRET=supersecretkey_change_in_production

# PostgreSQL connection (Option A: Cloud Database - Preferred)
DATABASE_URL=postgresql://user:password@host.aws.neon.tech/dbname?sslmode=require

# PostgreSQL connection (Option B: Local Database fallback)
PG_HOST=localhost
PG_PORT=5432
PG_DATABASE=pyscape
PG_USER=postgres
PG_PASSWORD=your_local_postgres_password 

# Email Verification (Gmail App Password)
EMAIL_USER=your_official_bot_gmail@gmail.com
EMAIL_PASS=your_16_char_app_password
